# Product Free RAG A6-7 런타임 적용 결과 — 2026-08-05

## 결론

최종 판정은 **PARTIAL**이다. R1과 R2는 각각 런타임에 적용했고 회귀
게이트는 통과했지만, A6-7 라이브 답변은 두 절 중 하나만 맞았다.

```text
정답  타이드 바운드 20초 → 18초
실제  타이드 바운드 12초 → 9초       (오답)

정답  질풍 개화 옵션 12초 → 9초
실제  질풍 개화 옵션 12초 → 9초       (정답)
```

근거 pack에는 두 정답이 모두 정확히 존재한다. 현재 남은 결함은 검색이나
atomic 경계가 아니라, Qwen이 E3 대신 E1을 두 요구에 중복 적용하는
**복수 요구-근거 결합 실패**다. 이 결과를 GO로 승격하지 않는다.

## R1 — 숫자 괄호 결합

- A6-7 `base_cooldown_change`: `value_present_none → value_present_full`
- 전체 49개 측정 요구:
  - M3: full 39 / partial 4 / none 6
  - R1: full 40 / partial 4 / none 5
- numeric/date/time/currency 감소: 0
- descriptive 진단 제외: A6-17, A6-29
- pack 집합 변경: A6-7, A6-13
- pack 순서만 변경: 0
- 인용 좌표 복원: 32/32
- 저장 출력의 exposed citation exact/verified: 32/32
- 후보 chunk ID 불변: 32/32
- chunk corpus SHA 불변:
  `bd0242b3f19646c22eafd2e61bf2544e670718a1c9927de288decb5657e92885`

R1 A6-7 라이브 전문:

```text
타이드 바운드 쿨타임은 12초에서 9초로 줄었고, 기본 쿨타임은 12초에서 9초로 바뀌었다.

질풍 개화 옵션의 기본 쿨타임은 12초에서 9초로 바뀌었다.
```

E3에는 `- 타이드 바운드 - 쿨타임이 감소합니다. (20초 → 18초)`가
정확히 들어갔지만 모델이 선택하지 않았다.

### A6-13 감시

기존:

```text
마이핀은 13자리 번호입니다.
마이핀의 유효기간은 3년입니다.
```

R1 후:

```text
마이핀은 13자리 번호입니다.
마이핀의 유효기간은 3년입니다.
한 해에 최대 5번 다시 발급받을 수 있습니다.
```

답변은 세 번째 사실까지 추가되어 개선됐고 인용은 exact다. 다만 기존
자동 채점기는 표현형 직접값 판정 때문에 `meaning_complete=false`를 유지했다.

## R2 — 표 도입문 결합과 ranking 분리

Qwen/verifier가 보는 `context_text`에는 안전한 표 도입문을 추가했다.
lexical prefilter, requirement score, BGE reranker 입력에서는 같은 도입문을
제거해 R1의 순위를 그대로 유지했다.

저장 출력 전수 replay:

- 저장 record: 1,302
- 고유 질문·후보 조합: 248
- context 변경 record: 367
- pack 집합 변경: 0
- pack 순서 변경: 0
- 좌표 불일치: 0
- Qwen 호출: 0

A6-7 E1의 최종 문맥:

```text
업데이트 > 개선 및 변경 사항 > 표 헤더: | 변경 전 | 변경 후 |
> 표 도입: - '질풍' 스킬 개화 옵션이 변경됩니다.
```

R2 A6-7 라이브 전문:

```text
타이드 바운드 쿨타임은 12초에서 9초로 줄었고,
질풍 개화 옵션의 기본 쿨타임은 12초에서 9초로 바뀌었다.
```

두 번째 절의 표 주어는 올바르게 결합됐지만, 모델은 한 claim에서 E1만
선택해 첫 번째 요구에도 `12초 → 9초`를 재사용했다. 따라서 사전 등록한
“두 절 모두 정답” 게이트는 실패다.

### 표 전수 정의 확인

- 전체 표: 1,532
- 라벨 컬럼 포함: 560
- 채택한 각주-중단 안전 도입문: 1,117
- 라벨과 안전 도입문이 모두 없는 표: 234

지시서의 `170`은 S1 안전 필터 전의 더 넓은 “도입문 후보” 정의에서 나온
수치다. 채택한 각주-중단 정책 기준으로는 234이며, 이 표들은 R2에서
context·좌표·선택이 모두 불변이다. 560개 라벨 표의 기존 헤더/표 대상
문맥도 유지된다.

## USER10 v2

이전 `product_pipeline_user10_v2_header_filter_20260805.jsonl`과 비교했다.

- mode 동일: 10/10
- evidence pack 좌표·순서 동일: 10/10
- 최종 답변 문자열 동일: 10/10
- 오류·timeout: 0
- 모드 분포: answer 6 / unsupported 4

## 회귀와 호출 수

- 전체 `tests/v3`: 1,240 passed, 기존 manifest SHA 실패 2건, 67 subtests
- 새 실패: 0
- Qwen 호출: 13
  - R1 A6-7 1
  - R1 A6-13 감시 1
  - R2 A6-7 1
  - USER10 v2 10
- A6 32문항 전체 라이브: 실행하지 않음
- 코퍼스/인덱스 재빌드: 없음

## 다음 실패 경계

R1과 R2를 더 확장할 문제가 아니다. 다음 진단은 동일 질문의 두 독립 요구가
각각 E3와 E1에 결합되도록 하는 requirement-to-evidence assignment 또는
생성 프롬프트 구조를 대상으로 해야 한다. 검색 점수, 도메인 어휘 허용목록,
verifier 완화로 우회하면 안 된다.
