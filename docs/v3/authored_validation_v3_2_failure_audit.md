# Authored validation v3.2 failure audit

This audit inspects the eight strict failures from the one-time authored
validation run. The set is now adaptive validation. Questions and gold remain
unchanged.

Each case receives exactly one earliest stage using the Q4 taxonomy. A cited
chunk can be marked `MEASUREMENT` only when its exact official text directly
answers every requested attribute. Such a chunk is an acceptable-sibling
**proposal**, not an applied gold edit. Strict and provisional adjudicated
metrics are reported together.

